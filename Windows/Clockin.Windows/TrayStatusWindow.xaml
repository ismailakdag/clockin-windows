<Window x:Class="Clockin.Windows.TrayStatusWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Width="300" Height="42" WindowStyle="None" ResizeMode="NoResize" ShowInTaskbar="False"
        Topmost="True" ShowActivated="False" WindowStartupLocation="Manual"
        Background="Transparent" AllowsTransparency="True">
  <Border Background="{DynamicResource CardBrush}" BorderBrush="{DynamicResource CardStrokeBrush}" BorderThickness="1" CornerRadius="11" Padding="9,4" Cursor="SizeAll" ToolTip="Drag to reposition · click to open" MouseLeftButtonDown="TrayStatus_MouseLeftButtonDown" MouseMove="TrayStatus_MouseMove" MouseLeftButtonUp="TrayStatus_MouseLeftButtonUp">
    <DockPanel LastChildFill="False">
      <Border Width="22" Height="22" CornerRadius="7" Background="{DynamicResource WindowBackgroundBrush}" DockPanel.Dock="Left" Margin="0,0,8,0"><TextBlock Text="◷" FontSize="14" Foreground="{DynamicResource AccentBrush}" HorizontalAlignment="Center" VerticalAlignment="Center"/></Border>
      <StackPanel VerticalAlignment="Center">
        <TextBlock x:Name="StatusText" Text="READY" FontSize="8" FontWeight="Black" Foreground="{DynamicResource MutedBrush}"/>
        <TextBlock x:Name="InfoText" Text="00:00:00" FontSize="10" FontFamily="Cascadia Mono" Margin="0,1,0,0"/>
      </StackPanel>
      <TextBlock x:Name="GoalText" DockPanel.Dock="Right" Text="" FontSize="8" Foreground="{DynamicResource AccentBrush}" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="8,0,0,0"/>
    </DockPanel>
  </Border>
</Window>
