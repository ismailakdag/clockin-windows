<Window x:Class="Clockin.Windows.PinnedWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Width="320" Height="112" MinWidth="246" MinHeight="72" MaxWidth="640" MaxHeight="500"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent" Topmost="True" ShowInTaskbar="False" ResizeMode="CanResize" MouseLeftButtonDown="DragWindow">
  <Border x:Name="PinnedShell" Background="{DynamicResource CardBrush}" Opacity="0.96" CornerRadius="18" BorderBrush="{DynamicResource CardStrokeBrush}" BorderThickness="1" Padding="15">
    <Grid>
      <Grid x:Name="CompactView" Visibility="Collapsed">
        <Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
        <Ellipse x:Name="CompactDot" Width="8" Height="8" Fill="{DynamicResource MutedBrush}" VerticalAlignment="Center" Margin="0,0,10,0"/>
        <StackPanel Grid.Column="1" VerticalAlignment="Center"><TextBlock x:Name="CompactStatus" Text="READY" FontSize="9" FontWeight="Bold" Foreground="{DynamicResource MutedBrush}"/><TextBlock x:Name="CompactElapsed" Text="00:00:00" FontFamily="Cascadia Mono" FontSize="23"/></StackPanel>
        <StackPanel Grid.Column="2" VerticalAlignment="Center" HorizontalAlignment="Right"><TextBlock x:Name="CompactMoney" Text="$0.00" FontSize="13" FontWeight="SemiBold" Foreground="{DynamicResource AccentBrush}" HorizontalAlignment="Right"/><TextBlock x:Name="CompactTry" Text="≈ ₺0.00" FontSize="9" Foreground="{DynamicResource MutedBrush}" HorizontalAlignment="Right"/></StackPanel>
      </Grid>
      <Grid x:Name="MoneyView" Visibility="Visible">
        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>
        <DockPanel><StackPanel Orientation="Horizontal"><Ellipse x:Name="Dot" Width="7" Height="7" Fill="{DynamicResource MutedBrush}" Margin="0,0,6,0" VerticalAlignment="Center"/><TextBlock x:Name="PinnedStatus" Text="READY" FontSize="8" FontWeight="Black" Foreground="{DynamicResource MutedBrush}"/></StackPanel><TextBlock x:Name="PinnedElapsed" Text="00:00:00" FontFamily="Cascadia Mono" FontSize="16" HorizontalAlignment="Right" DockPanel.Dock="Right"/></DockPanel>
        <DockPanel Grid.Row="1" Margin="0,7,0,0"><TextBlock x:Name="PinnedMoney" Text="$0.00" FontSize="22" FontWeight="Bold" Foreground="{DynamicResource AccentBrush}"/><TextBlock x:Name="PinnedTry" Text="≈ ₺0.00" FontSize="14" FontWeight="SemiBold" Foreground="{DynamicResource TextBrush}" HorizontalAlignment="Right" DockPanel.Dock="Right"/></DockPanel>
        <TextBlock x:Name="PinnedMomentum" Grid.Row="2" Text="+$0.0069/sec" FontFamily="Cascadia Mono" FontSize="8" Foreground="{DynamicResource AccentBrush}" Margin="0,7,0,0"/>
      </Grid>
      <Grid x:Name="GoalView" Visibility="Collapsed">
        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition/><RowDefinition/></Grid.RowDefinitions>
        <DockPanel><StackPanel Orientation="Horizontal"><TextBlock Text="◎" FontSize="13" Foreground="{DynamicResource AccentBrush}" Margin="0,0,6,0"/><TextBlock Text="GOAL MODE" FontSize="9" FontWeight="Black" Foreground="{DynamicResource MutedBrush}"/></StackPanel><TextBlock x:Name="GoalElapsed" Text="00:00:00" FontFamily="Cascadia Mono" FontSize="14" DockPanel.Dock="Right"/></DockPanel>
        <StackPanel Grid.Row="1" Margin="0,7,0,0"><DockPanel><TextBlock Text="TODAY" FontSize="8" Foreground="{DynamicResource MutedBrush}"/><TextBlock x:Name="GoalTodayText" Text="0m / 0m" FontSize="9" HorizontalAlignment="Right" DockPanel.Dock="Right"/></DockPanel><ProgressBar x:Name="GoalTodayProgress" Height="5" Maximum="1" Margin="0,3,0,6" Foreground="{DynamicResource AccentBrush}" Background="{DynamicResource CardStrokeBrush}"/></StackPanel>
        <StackPanel Grid.Row="2"><DockPanel><TextBlock Text="MONTH" FontSize="8" Foreground="{DynamicResource MutedBrush}"/><TextBlock x:Name="GoalMonthText" Text="0m / 0m" FontSize="9" HorizontalAlignment="Right" DockPanel.Dock="Right"/></DockPanel><ProgressBar x:Name="GoalMonthProgress" Height="5" Maximum="1" Margin="0,3,0,0" Foreground="{DynamicResource SecondaryAccentBrush}" Background="{DynamicResource CardStrokeBrush}"/></StackPanel>
      </Grid>
      <ScrollViewer x:Name="AllView" Visibility="Collapsed" VerticalScrollBarVisibility="Auto" MaxHeight="470">
        <StackPanel>
          <DockPanel><StackPanel Orientation="Horizontal"><Ellipse x:Name="AllDot" Width="7" Height="7" Fill="{DynamicResource MutedBrush}" Margin="0,0,6,0" VerticalAlignment="Center"/><TextBlock x:Name="AllStatus" Text="READY" FontSize="8" FontWeight="Black" Foreground="{DynamicResource MutedBrush}"/></StackPanel><TextBlock x:Name="AllElapsed" Text="00:00:00" FontFamily="Cascadia Mono" FontSize="16" HorizontalAlignment="Right" DockPanel.Dock="Right"/></DockPanel>
          <DockPanel Margin="0,7,0,0"><TextBlock x:Name="AllMoney" Text="$0.00" FontSize="23" FontWeight="Bold" Foreground="{DynamicResource AccentBrush}"/><TextBlock x:Name="AllTry" Text="≈ ₺0.00" FontSize="14" HorizontalAlignment="Right" DockPanel.Dock="Right"/></DockPanel>
          <TextBlock x:Name="AllPerSecond" Text="+$0.0069/sec" FontFamily="Cascadia Mono" FontSize="8" Foreground="{DynamicResource AccentBrush}" Margin="0,7,0,8"/>
          <UniformGrid Columns="3" Margin="0,0,0,8"><Border Background="{DynamicResource BlackOverlayBrush}" CornerRadius="7" Padding="6" Margin="2"><StackPanel><TextBlock Text="AVG DAY" FontSize="7" Foreground="{DynamicResource MutedBrush}"/><TextBlock x:Name="AllAvgDay" FontSize="9" FontFamily="Cascadia Mono"/></StackPanel></Border><Border Background="{DynamicResource BlackOverlayBrush}" CornerRadius="7" Padding="6" Margin="2"><StackPanel><TextBlock Text="AVG WEEK" FontSize="7" Foreground="{DynamicResource MutedBrush}"/><TextBlock x:Name="AllAvgWeek" FontSize="9" FontFamily="Cascadia Mono"/></StackPanel></Border><Border Background="{DynamicResource BlackOverlayBrush}" CornerRadius="7" Padding="6" Margin="2"><StackPanel><TextBlock Text="AVG MONTH" FontSize="7" Foreground="{DynamicResource MutedBrush}"/><TextBlock x:Name="AllAvgMonth" FontSize="9" FontFamily="Cascadia Mono"/></StackPanel></Border></UniformGrid>
          <StackPanel x:Name="AllGoalsPanel"/>
          <DockPanel Margin="0,8,0,0"><TextBlock x:Name="AllRadioIcon" Text="♫" FontSize="13" Foreground="{DynamicResource MutedBrush}" Margin="0,0,6,0"/><TextBlock x:Name="AllRadioText" Text="FOCUS RADIO OFF" FontSize="8" FontWeight="Bold"/><Button x:Name="AllRadioButton" Content="▶" Width="26" Height="24" Padding="0" HorizontalAlignment="Right" DockPanel.Dock="Right" Click="Radio_Click"/><Slider x:Name="AllRadioVolume" Width="65" Minimum="0" Maximum="1" Margin="8,0,0,0" HorizontalAlignment="Right" DockPanel.Dock="Right" ValueChanged="RadioVolume_Changed"/></DockPanel>
        </StackPanel>
      </ScrollViewer>
    </Grid>
  </Border>
</Window>
